{
  "name": "acik-athletics-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.48.0",
    "zod": "^3.24.0"
  }
}
